# Windows 桌面客户端下载与使用文档

2026-09-06：已按确认的 UI 接入网站代码，仅支持 Windows x64。统一命名为「桌面客户端」，不展示 macOS / Linux 下载卡片或支持承诺。本次为功能实现及本地验证，未部署。

## 页面与入口

- 主导航：TokenBoard / 客户端下载 / 使用文档。个人数据入口保留，并改用统计图标和对应可访问名称。
- 排行榜侧栏：「你的数据」之后增加客户端接入卡片，提供下载和快速开始入口。小屏幕卡片收在侧栏第一位。
- 页脚：客户端下载、使用文档、数据与隐私。
- `/download`：Windows 桌面客户端下载、带示例标记的面板示意、安装指南和隐私简述。
- `/docs`：跳转 `/docs/quickstart`。
- `/docs/:slug`：快速开始、安装与运行、支持的工具、采集与同步、数据与隐私、常见问题、发布说明。无效文档显示 404。

复用既有 React Router、登录上下文和中英文语言切换。下载与文档不要求登录。文档采用桌面左导航 / 正文 / 右侧页内目录，小屏幕折叠为顶部换行导航；页内目录支持定位，FAQ 使用原生折叠面板。

## 最新版本规则

`web/src/pages/resources/windowsRelease.ts` 每次进入下载页读取站点的 `releases/stable.json`，版本不写在页面中。桌面更新器读取同一份第一方 HTTPS 清单，不再依赖 GitHub Releases。

按 Windows x64 的 `major.minor.patch` 数字版本选择最高版本，预览版标记仅用于展示；最新记录缺包或校验信息时显示获取失败，不静默回退旧版本。

版本号、发布日期、预览标记、文件大小、可选 ZIP、更新说明与 SHA-256 均来自同一记录。包地址由内部发布流程维护，为永久的 OSS/CDN HTTPS 地址。无需凭据，不携带网站会话。

获取超时为 10 秒，退出页面时取消请求。加载中、尚无发行版和获取失败分别显示状态。失败时可重试；校验值和更新说明直接在下载页展示，不再跳转私有 GitHub 页面。具体发布流程见 [桌面版本发布](desktop-release-publishing.md)。

## 文档口径

- 目前仅支持 Windows x64。桌面客户端统一连接多种 AI 工具，无需逐个安装扩展。
- 隐藏面板不停止后台采集；暂停采集同时暂停同步，退出登录或会话失效停止后续上传。
- 排行榜按 UTC，本机面板按本地日历日；全部时间指本机仍有记录的全部历史。
- 不上传提示词、回复和代码正文；部分额度查询会使用本机有效登录状态，但凭据不上传到 TokenDance 网站。
- 排行榜展示头像、昵称、Token 和排名，详细资料页公开开关不隐藏榜单数据。
- Token、额度和费用是独立能力；未知和不支持不能当成零。

说明依据当前仓库 README、桌面客户端文档与公开发行说明。发行包签名状态、兼容性及已知问题以具体发行说明为准。

## 验证

- TypeScript 检查与 Vite 构建通过。
- 下载文档、导航、排行榜刷新、本人排名、子路径相关测试 22 项通过。
- 新增测试覆盖语义版本排序、预览版、草稿与带后缀标签排除、最新版缺包不降级、下载地址与 hash 校验、限流后重试、空状态、中英切换、子路径链接、FAQ 定位和文档 404。
- 浏览器实际读取最新 Windows 发行记录，确认双下载入口同步更新；开发过程中版本从 0.1.15 更新至 0.1.16，页面自动读取到新版本。
- 桌面及 390px 窄屏检查：下载与文档无横向溢出，页内跳转避开固定导航，排行榜入口可正常进入下载页。

## 历史设计稿

`ui-prototypes/26-download-and-docs.html` 与 26–29 号截图保留第一轮设计参考，当时的 macOS 占位和“下载待发布”状态已被本次 Windows-only 实现取代。当前预览应使用网站 `/download` 与 `/docs/quickstart` 路由。

后续发布仍须遵守仓库部署规则：从干净 main 构建，HEAD 等于 origin/main，发布记录包含 main 和完整 SHA。本次未执行发布。
