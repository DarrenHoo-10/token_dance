# 测试服务发布记录 · 2026-09-12

- 入口：https://nexorai.com.cn/token-dance-test/
- 构建分支：`release`，已推送 `origin/release`。
- 完整提交：`5f8cdef2c27d047f2695927fb23aec6142952703`。
- 构建时间：2026-09-12 06:22:47 UTC；切换时间：2026-09-12 06:24:01 UTC。
- 源码：从 `codex/team-init` 的团队功能、CR-015 / CR-016 修复及测试发布配置创建独立 release worktree，干净构建。
- 版本目录：`/opt/token-dance-test/releases/20260912-062247-5f8cdef2`。
- 构建包 SHA-256：`2de049c9a579890f4f4254d46d015830142d58ca286d353f621eb4508f91075a`。
- 备份：`/var/backups/token-dance-test/20260912-062401/database.sql`，仅服务器保存；同目录 `release.json` 保存发布元数据。

## 环境与数据

独立的 `token-dance-test-api` / `token-dance-test-worker` 已启用并运行，API 仅监听 `127.0.0.1:8131`。配置位于 `/etc/token-dance-test`，使用专用系统账号和仅有 `tokendance_dev.*` 权限的数据库账号。

测试库从 7 个迁移推进至 13 个，checksum、dirty 状态和 schema compatibility 检查全部通过；保留现有 11 个账号。迁移 0009 按设计清理可重建的旧日统计和排行榜投影，不删除原始用量和用户/团队数据；新版团队分析读取 v2 上传事实。灰度同步在备份/迁移期间短暂停止，发布后恢复 active。

Redis 使用 `redis_dev`，对象存储使用独立 `token-dance-test/` 前缀。邮件 sink 开启，固定测试验证码关闭。复用原测试库 email lookup、auth subject、AEAD 兼容密钥以保留账号登录，其他操作密钥独立生成。凭据和测试账号密码没有进入 Git 或发布包。

测试服务使用 apex 主机名，生产保持 `www.nexorai.com.cn`。TLS 复用现有覆盖两者的证书。测试 Cookie 保持 Path=/，由 Nginx 设置 Secure / HttpOnly / SameSite=Lax。Nginx 原有其他站点重复 server_name 警告仍存在，配置检测及本次测试/生产入口验证成功。

## 实际验收

- 前端从 release worktree 使用 `/token-dance-test/` base 构建成功，Linux amd64 API / Worker / migrate / checkproviders 构建成功。
- 两个测试服务 active、NRestarts=0；生产 API / Worker 和灰度同步 active。
- 测试及生产 HTTPS readyz 均为 200；测试 `build-info.json` 返回实际 release 分支与上述完整 SHA。
- 浏览器使用本地私有账号文档中的测试账号登录成功；确认会话只属于测试主机名且 Secure、HttpOnly。
- 已验证注册深链接刷新、未登录创建团队的登录保护、团队入口、创建临时团队、成员 API、概览页面及异步分析快照 ready。
- 冒烟临时团队已解散，测试账号恢复未加入团队；没有修改既有团队。
- 修正冒烟脚本误读响应 `status` 为 `state` 后重跑通过；这是本地测试脚本断言修正，未变更发布的应用。
- 浏览器没有运行时异常或 5xx 响应。
- 对象存储写入、HEAD、签名下载和删除通过。通用 checkproviders 随后因测试环境未配置 SMTP 返回非零，符合本环境 sink 配置；不计为 SMTP 验证通过，也没有发送邮件。

截图与本地脚本位于忽略目录 `build/test-release/`。部署规则及后续操作见 [测试发布说明](../deploy/test/README.md)。生产版本和生产数据库未发布或迁移。

## 测试登录修复

2026-09-12：仅设置镜像账号密码仍无法登录，因为账号没有测试邮箱绑定，且原灰度同步每次更新都会清空邮箱字段。

- 从干净 `release` 分支提交 `b1ecdd5e78fe4e80b3a8c297c7b8abe5d045e47d` 构建并更新灰度同步组件；API、Worker 和前端仍为上文版本。
- 新账号继续使用脱敏身份；已有账号更新时保留测试库的邮箱密文、邮箱哈希、认证主体哈希及邮箱验证时间，防止覆盖测试登录绑定。
- 组件 SHA256：`fe4205882c004b19155115cb6ba189663d4de8a6933fcf7e327011b33c883619`。
- 发布及私有绑定备份：服务器 `/var/backups/token-dance-test/20260912-065529-login-binding/`。
- 灰度包单元测试通过；实际完整同步后四个身份字段保持不变；浏览器使用用户指定凭据登录返回 200，并验证会话身份、团队接口、页面及刷新。三个相关服务均 active。
- 具体邮箱和密码仅记录在仓库外的本地账号文档，不写入 Git。

## 团队入口与加载提示修正

2026-09-12 07:17:07 UTC，从干净 `release` 分支提交 `1c6a3cd0cae81b0962edc8f94c870aa8d81538dd` 构建并发布。

- 顶部新增团队入口，在团队子页面保持选中；首次汇总使用中性的加载文案，避免误报共享范围已变更。
- 截图状态排查：实测接口从 202 updating 约 2 秒后转为 200 ready，未复现持续卡住。当前范围没有已授权用量时显示空状态，不填造数据。
- 7 项分析及多语言测试、类型检查、前后端构建通过。实际浏览器验证桌面和 390px 手机宽度下的团队 Tab 点击、选中状态、总览加载完成、登录及刷新；无横向溢出或运行时异常。
- 发布包 SHA256：`ccfb70428b0eb4ba44e96c985bef16e47b58dac43616686ad747eb1aeb7a8e96`；服务器备份 `/var/backups/token-dance-test/20260912-071707/`。
- 测试 build-info 与提交一致，测试及生产 readyz 均为 200。私有验证截图保存在忽略目录 `build/test-release/`。

## 历史统计与汇总性能

2026-09-12 07:32:44 UTC，从干净 `release` 分支提交 `36af2b539c5042e6da5d421dafc986a9637845f7` 构建并发布。

- 按用户要求，规则版本升级为 3：当前有效共享授权覆盖所选日期内的历史数据，取消加入时间及授权开始时间截断。已关闭/撤销授权及已退出成员仍然排除，旧规则快照和导出不再复用。
- 原路径为两秒 Worker 串行调度、两秒前端轮询、30 秒后无条件重算。现在团队汇总独立每 500ms 调度，未就绪轮询 500ms；数据版本未变且快照未过期时复用，有新版本时保留同授权结果并后台刷新。
- 同一测试环境、同一团队 30 天统计：旧版首次等待 2117ms，新版首次等待 1119ms；35 秒后返回 200 ready 耗时 63ms，快照 ID 未变。一次首次生成样本不能代表所有数据规模或网络情况。
- 人工递增一次该测试团队的数据版本，验证已有结果在 66ms 内返回 200 ready / refreshing，随后后台发布新快照，整个过程中未退回 202。只更新刷新版本号，没有添加或修改成员用量。
- HTTP API、团队服务、Worker、内存及 MySQL 包本地测试通过，相关 vet、前端 7 项分析/多语言测试、类型检查及构建通过；未配置 MySQL 集成测试 DSN 的用例仍跳过，不视为数据库集成全通过。实际测试库读缓存、排队、后台刷新及浏览器登录、桌面/手机团队页面通过。
- 发布包 SHA256：`3e8e54fb02023530a77be1aa7d76969b4ee109a565d0501112aec2f90693fc4f`；服务器备份 `/var/backups/token-dance-test/20260912-073244/`。测试和生产 readyz 均为 200，生产未发布。

## 自定义日期无限加载与实际阶段耗时

2026-09-12 07:48:31 UTC，最终发布为干净 `release` 分支提交 `aae089eb0009d10fbd186fc8cfdc66753bbb185a`。

- 复现用户完整路径 `?range=custom`：没有起止日期，统计请求数为 0，骨架屏为 1，日期输入框为 0。这是前端状态错误导致的永久等待，前述 30 天接口 1.1 秒样本不适用于该路径。
- 总览和分析页均增加等待日期状态，加载/错误时保留日期选择器；补齐日期后自动查询，清空日期和切回预设范围均可恢复。
- 同时修复空统计返回省略 trend 数组导致分析页崩溃的问题，服务端返回空数组，前端兼容旧服务省略该字段。
- 接口新增 Server-Timing；后台日志记录队列年龄、读取与汇总、写入、发布耗时，不记录邮箱、密码或原始用量内容。
- 一次未缓存自定义范围的浏览器实测：首次请求 56ms，固定轮询等待约 500ms，第二次请求 59ms，再到下一渲染帧约 16ms；从结束日期 input 事件到结果帧为 631ms。缓存命中样本为 76ms。独立自动化断言检测耗时含工具等待，不再当作实际渲染耗时。
- 上述同一快照的后台计时：队列年龄 170.929ms，读取与汇总 4.861ms，结果写入 0ms（空结果无明细写入），发布 6.076ms，实际后台处理 10.938ms。后台排队/处理与前端的 500ms 等待重叠，不能重复相加；这是当前无原始用量测试团队的样本，不代表大数据量性能。
- 10 项前端测试、类型检查、相关团队服务/HTTP API/Worker 测试及 vet 通过。真实浏览器验证两页缺日期深链接、补齐、刷新、清空、切回 7 天和手机日期控件，无运行时错误。测试/生产 readyz 均为 200。
- 发布包 SHA256：`1c22aeaef6769459192b685f594dcc445f5ecd20a61aba19d441bb155ddfbd08`；备份 `/var/backups/token-dance-test/20260912-074831/`；私有计时和截图位于忽略目录 `build/test-release/`。

## 默认今天与自定义日期限制

2026-09-12 07:57:59 UTC，干净 `release` 分支提交 `6b5341d2b9c55dd989ee8a3bf516426b1d8ccd70` 已发布。

- 总览及用量分析使用今天、7 天、30 天、自定义四个选项，缺省页面参数和接口参数均默认今天，按团队时区的本地零点划分。
- 自定义可选历史日期，最多 90 个自然日（含首尾），不是最近 90 天的回溯限制。页面显示说明，日期控件限制未来日期，服务端同时校验起止顺序、长度和未来结束日期。
- 前端 10 项测试与类型检查、团队/HTTP API 相关测试通过；新增默认时区、夏令时 23 小时自然日、90/91 天及未来结束日期回归。浏览器验证两页默认值、四选项顺序及切换、无日期自定义、提示、手机布局；实际接口验证缺省今天、90 天可接受、91 天和未来日期返回 400。
- 发布包 SHA256：`ee55b3d524673bc70f8049b1ec71b79b111e9f955fe2328f5a71480871b3f745`；备份 `/var/backups/token-dance-test/20260912-075759/`。测试 build-info 与 SHA 一致，测试/生产 readyz 均为 200。
