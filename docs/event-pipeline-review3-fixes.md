# 第三轮 CR 修复与验证

验证日期：2026-09-12。修复分支：`codex/fix-event-pipeline-review3`。

基线为客户端 `838c4b317d7ac28c1fb92e6082b8be4370321c35`，合入服务端及 Web 多币种分支 `ab636c19934c6a037a26a67fb3035cffc9e5c8ca` 后修复。基线合并提交为 `33a12b3e7a240b93ef1a915ef8faba5f23d22969`。本文记录工作区修复结果，不代表发布记录。

## 修复内容

| 问题 | 修复后的行为 | 回归覆盖 |
| --- | --- | --- |
| Codex 会话归档后路径变化，重复生成事件 | 从原生 `session_meta.payload.id` 获取会话身份，并随采集游标保存解码上下文；归档前后使用相同事件幂等键。无原生头的格式继续保留文件隔离 | 先单独提交文件头，再读用量；归档、取消归档后仍只有一条事件 |
| 多目录发现游标反复回绕，漏掉部分文件 | 所有 Codex 根目录合并排序、去重后统一分页 | 活跃及归档目录各 40 个文件，分页大小为 1、31、64 时全部发现 |
| 卡片与悬浮球读取不同账本 | 新管线启用时，两者统一读取新统计表；新表为空时显示未知，不回退旧账本 | 旧账本 15、新管线 42；仅有旧数据；新管线已知零值 |
| 卡片丢失新管线费用与定价覆盖信息 | 在一次 writer 命令中读取日用量、费用及小时序列；上报费用与按价格表计算的费用分别传递，保留币种和未定价计数；累计值覆盖全部日历史 | USD/CNY、已知零费用、避免 USD 兼容字段重复累加、超过图表窗口的累计用量 |
| 恢复备份只更新旧采集器 | 复用统一的开关应用入口，同时更新运行时管线 | 从启用恢复为停用，以及反向恢复 |
| 最早的 32 个来源被单一 harness 占满 | 按 harness 查询候选，先各分配一个位置，再分配第二个；逐轮旋转起始 harness。保留全局 4、每 harness 2 的并发上限 | Codex 积压时 Claude 正常运行；6 个 harness 均有积压时全部取得进展 |
| 服务端把已知零费用返回 null | 按已知样本区分零与未知；整数金额直接转换为 8 位小数字符串，避免浮点精度损失 | 真实 MySQL 查询零费用、未定价和超过 JS 安全整数范围的金额；Web 展示 `$0.00` |

未增加业务表或索引，未调整数据迁移、保留周期和发布版本表。采集候选查询命中现有 `idx_sources_due`；日用量查询命中 `idx_model_filter`。费用查询使用现有唯一索引筛选日粒度并分组，没有读取事件明细表。

## 验证结果

| 验证 | 结果 |
| --- | --- |
| 桌面 Rust 库测试 | 223 通过、0 失败；测试框架报告 7 个原有忽略项，其中部分进程辅助入口由父测试显式运行 |
| 桌面用量、费用等前端测试 | 38 通过 |
| 桌面 TypeScript 与 Vite 构建 | 通过 |
| Web 费用、API、仪表盘测试 | 27 通过 |
| Web TypeScript 检查 | 通过 |
| 服务端 `go test ./... -count=1` | 通过；此轮本地进程不配置集成测试 DSN，数据库用例另行验证 |
| 云端 MySQL 集成与相关回归 | 15 个顶层用例通过，包含入库幂等与冲突、时间窗口、设备绑定隔离、并发聚合、重放、多币种、缓存命中口径、零费用和精确金额 |
| `git diff --check` | 通过 |

云端测试使用开发 MySQL 实例中新建的隔离库及仅有该库权限的临时账号。测试二进制经 SSH 在临时目录执行，未部署服务；测试结束已删除隔离库、账号及二进制。既有开发数据与生产库均未修改，未使用 Redis。连接凭据没有进入源码、报告或测试日志。

桌面测试使用隔离的用户目录；本机代理会拦截回环 HTTP 测试，因此仅为测试进程设置 `NO_PROXY=localhost,127.0.0.1,::1`。实时额度、公开价格目录、线上更新源等需显式启用的外部检查未在本轮开启。
