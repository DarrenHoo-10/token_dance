# 部署规则

- 仅允许部署 `main` 分支，禁止直接部署功能分支。
- 部署前拉取远端最新状态，确认改动已合入 `origin/main`；从干净的 `main` 工作区构建，构建时 `HEAD` 必须等于 `origin/main`。
- 发布记录须包含分支 `main` 和完整提交 SHA；功能分支测试通过或已推送，不代表允许直接部署。
- 此规则适用于后续所有部署，包括开发环境、生产环境和桌面端发布。

# 升版本规则

- 修改版本号之前，必须先查询发布版本库 `desktop_releases` 的历史记录，并核对远端 Git 标签、GitHub Releases（含草稿和预发布）及正在进行的发版 PR，确认候选版本号未被占用。
- 检查须覆盖 Windows、Mac 各架构及相关发布环境；不能仅依据本地 Cargo/Tauri 版本号或只保留当前版本的公开清单判断。
- 新改动使用高于已发布版本、且未被其他发版占用的新版本号。已使用的版本号不能重新承载不同源码或覆盖安装包；同一发行补齐平台包必须核对相同的 main 提交 SHA。
- 无法查询版本库或发布记录时，不能把“查询失败”视为“版本不存在”。在版本变更 PR 中记录查询时间、查询范围、候选版本与检查结果，发布前再次检查。
- 具体步骤见 [桌面版本发布文档](docs/desktop-release-publishing.md#升版本前先查版本库)。

# PR 与 CI 检查

- 创建 PR 或向 PR 推送新提交后，必须主动查看 GitHub CI，并等待最新 PR head 提交的所有适用检查通过；本地测试通过不能代替 CI。
- 合并前再次核对提交 SHA 和检查结果。失败、取消、超时、排队、运行中或缺少必需检查都不算通过；只有工作流明确不适用的条件任务才允许跳过。
- CI 失败时必须读取失败步骤的日志、修复原因、推送并重新检查，直到通过。不能靠跳过失败测试、放宽校验或设置 `continue-on-error` 来变绿。
- 在 PR 或交付记录中保留已验证提交 SHA、CI 结果及运行链接。步骤见 [PR 创建后的 CI 检查](docs/desktop-release-publishing.md#pr-创建后的-ci-检查)。
