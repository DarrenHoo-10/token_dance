# 部署规则

- 仅允许部署 `master` 分支，禁止直接部署功能分支。
- 部署前拉取远端最新状态，确认改动已合入 `origin/master`；从干净的 `master` 工作区构建，构建时 `HEAD` 必须等于 `origin/master`。
- 发布记录须包含分支 `master` 和完整提交 SHA；功能分支测试通过或已推送，不代表允许直接部署。
- 此规则适用于后续所有部署，包括开发环境、生产环境和桌面端发布。
